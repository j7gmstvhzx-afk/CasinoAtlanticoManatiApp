// Promoted to `@/components/ui` as a shared primitive — re-exported here so
// existing relative imports (`from './AnimatedPressable'`) keep working.
export { AnimatedPressable } from '@/components/ui/AnimatedPressable';
